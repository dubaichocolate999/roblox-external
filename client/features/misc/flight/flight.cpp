#include "flight.h"

g_flight flight;

void g_flight::loop() {
    //matrix camCFrame = read<matrix>(Camera + offsets::CameraRotation);

    //vector_3 look_vector = vector_3(-camCFrame.data[0][2],
       // -camCFrame.data[1][2],
        //-camCFrame.data[2][2]);

    //vector_3 right_vector = vector_3(camCFrame.data[0][0],
       // camCFrame.data[1][0],
       // camCFrame.data[2][0]);


}